<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TradeWeb - 贸易提单RWA数字化平台</title>
  <link rel="stylesheet" href="https://cdn.bootcdn.net/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    /* 全局重置 - 移除默认边框/填充 */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Microsoft Yahei", sans-serif;
      border: none !important; /* 全局移除边框 */
      outline: none !important;
    }
    body {
      /* 主背景：低饱和灰蓝 → 深蓝渐变（无填充） */
      background: linear-gradient(120deg, #2c3e50 0%, #1a2530 100%);
      color: #ffffff; /* 全局字体白色 */
    }

    /* 顶部导航栏 - 更深的蓝灰色，无填充/边框 */
    .top-nav {
      background: #141e29; /* 深灰蓝色（比主背景更深） */
      box-shadow: none; /* 移除阴影（原边框类视觉元素） */
      padding: 0 50px;
      height: 70px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 999;
    }
    /* 平台Logo - 白色字体，无填充 */
    .logo {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 0; /* 移除填充 */
    }
    .logo-icon {
      font-size: 24px;
      color: #ffffff; /* 图标白色 */
    }
    .logo-text {
      font-size: 20px;
      font-weight: 600;
      color: #ffffff; /* Logo文字白色 */
    }
    /* 导航菜单 - 白色字体，无填充/边框 */
    .nav-menu {
      display: flex;
      gap: 40px;
      padding: 0; /* 移除填充 */
    }
    .nav-item {
      position: relative;
      text-decoration: none;
      color: #ffffff; /* 导航文字白色 */
      font-size: 16px;
      font-weight: 500;
      padding: 0 5px; /* 仅保留少量内边距（无填充感） */
      transition: color 0.3s ease;
    }
    .nav-item:hover {
      color: #e0e6ed; /* hover时浅白（无边框高亮） */
    }
    .nav-item.active {
      color: #ffffff;
    }
    .nav-item.active::after {
      /* 移除底部边框高亮，改为文字加粗 */
      content: '';
      display: none; /* 彻底移除原边框样式 */
    }
    .nav-item.active {
      font-weight: 700; /* 激活项加粗替代边框 */
    }
    /* 登录按钮 - 蓝灰色适配，无边框/填充 */
    .login-btn {
      padding: 8px 20px;
      background: #1f3041; /* 按钮蓝灰色（与导航栏呼应） */
      color: #ffffff; /* 按钮文字白色 */
      border-radius: 4px;
      font-size: 14px;
      font-weight: 500;
      cursor: pointer;
      transition: background 0.3s ease;
      padding: 0; /* 移除填充，仅保留内边距 */
      width: 120px;
      height: 40px;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .login-btn:hover {
      background: #273b4e; /* hover时稍浅的蓝灰色 */
    }

    /* 主体视觉区域 - 无填充/边框，白色字体 */
    .hero-section {
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: transparent; /* 继承body渐变背景，无额外填充 */
      margin-top: 70px;
      padding: 0 50px;
      text-align: center;
    }
    .hero-content {
      max-width: 800px;
      padding: 0; /* 移除填充 */
    }
    .hero-title {
      font-size: 48px;
      color: #ffffff; /* 标题白色 */
      margin-bottom: 20px;
      line-height: 1.3;
    }
    .hero-title span {
      color: #e0e6ed; /* 高亮文字浅白（无色彩对比，保持统一） */
    }
    .hero-desc {
      font-size: 18px;
      color: #ffffff; /* 描述文字白色 */
      line-height: 1.6;
      margin-bottom: 40px;
    }
    .start-btn {
      padding: 15px 40px;
      background: #1f3041; /* 按钮与导航栏同色系蓝灰 */
      color: #ffffff; /* 按钮文字白色 */
      border-radius: 8px;
      font-size: 18px;
      font-weight: 500;
      cursor: pointer;
      box-shadow: none; /* 移除阴影（原边框类视觉） */
      transition: background 0.3s ease;
      height: 60px;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 0 40px; /* 移除填充，用内边距控制大小 */
    }
    .start-btn:hover {
      background: #273b4e; /* hover时稍浅 */
    }

    /* 响应式适配 - 移动端保持风格统一 */
    @media (max-width: 768px) {
      .top-nav {
        padding: 0 20px;
      }
      .nav-menu {
        gap: 20px;
      }
      .logo-text {
        font-size: 18px;
      }
      .nav-item {
        font-size: 14px;
      }
      .hero-title {
        font-size: 32px;
      }
      .hero-desc {
        font-size: 16px;
      }
      .start-btn {
        height: 50px;
        padding: 0 30px;
        font-size: 16px;
      }
    }
  </style>
</head>
<body>
  <!-- 顶部导航栏 - 深灰蓝色，无填充/边框 -->
  <div class="top-nav">
    <!-- 平台Logo - 白色 -->
    <div class="logo">
      <i class="fa-solid fa-file-invoice logo-icon"></i>
      <div class="logo-text">TradeWeb</div>
    </div>
    
    <!-- 核心导航菜单 - 白色字体 -->
    <div class="nav-menu">
      <a href="index.html" class="nav-item active">首页</a>
      <a href="bill.html" class="nav-item">数字提单化</a>
      <a href="nft.html" class="nav-item">NFT铸造</a>
      <a href="finance.html" class="nav-item">融资管理</a>
      <a href="personal.html" class="nav-item">个人中心</a>
      <a href="team.html" class="nav-item">团队介绍</a>
      <a href="help.html" class="nav-item">帮助中心</a>
    </div>
    
    <!-- 登录按钮 - 蓝灰色，白色文字 -->
    <button class="login-btn" onclick="window.location.href='login.html'">登录/注册</button>
  </div>

  <!-- 主体视觉区域 - 渐变背景，白色字体，无填充 -->
  <div class="hero-section">
    <div class="hero-content">
      <h1 class="hero-title">贸易提单<span>RWA数字化</span>解决方案</h1>
      <p class="hero-desc">
        一站式实现提单上传、结构化提取、NFT铸造与融资管理，
        打造高效、安全的贸易金融数字化平台
      </p>
      <button class="start-btn" onclick="window.location.href='bill.html'">立即体验</button>
    </div>
  </div>
</body>
</html>