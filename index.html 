<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TradeWeb - 贸易提单RWA数字化平台</title>
  <link rel="stylesheet" href="https://cdn.bootcdn.net/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    /* 全局重置 */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Microsoft Yahei", sans-serif;
    }
    body {
      background: #285dab;
      color: #cdc6eb;
    }

    /* 顶部导航栏 - 核心导航区域 */
    .top-nav {
      box-shadow: 0 2px 10px rgba(237, 234, 240, 0.927);
      padding: 0 50px;
      height: 70px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 999;
    }
    /* 平台Logo */
    .logo {
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .logo-icon {
      font-size: 24px;
      color: #2c4666;
    }
    .logo-text {
      font-size: 20px;
      font-weight: 600;
      color: #c7d8e9;
    }
    /* 导航菜单 */
    .nav-menu {
      display: flex;
      gap: 40px;
    }
    .nav-item {
      position: relative;
      text-decoration: none;
      color: #555;
      font-size: 16px;
      font-weight: 500;
      padding: 0 5px;
      transition: color 0.3s ease;
    }
    .nav-item:hover {
      color: #4096ff;
    }
    .nav-item.active {
      color: #4096ff;
    }
    .nav-item.active::after {
      content: '';
      position: absolute;
      bottom: -8px;
      left: 50%;
      transform: translateX(-50%);
      width: 20px;
      height: 3px;
      background: #4096ff;
      border-radius: 3px;
    }
    /* 登录按钮 */
    .login-btn {
      padding: 8px 20px;
      background: #4096ff;
      color: #ffffff;
      border: none;
      border-radius: 4px;
      font-size: 14px;
      font-weight: 500;
      cursor: pointer;
      transition: background 0.3s ease;
    }
    .login-btn:hover {
      background: #3088ff;
    }

    /* 主体视觉区域 */
    .hero-section {
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(120deg, #e8f4f8 0%, #f0f8fb 100%);
      margin-top: 70px;
      padding: 0 50px;
      text-align: center;
    }
    .hero-content {
      max-width: 800px;
    }
    .hero-title {
      font-size: 48px;
      color: #b5cbe1;
      margin-bottom: 20px;
      line-height: 1.3;
    }
    .hero-title span {
      color: #4096ff;
    }
    .hero-desc {
      font-size: 18px;
      color: #d5d2e6;
      line-height: 1.6;
      margin-bottom: 40px;
    }
    .start-btn {
      padding: 15px 40px;
      background: #4096ff;
      color: #ffffff;
      border: none;
      border-radius: 8px;
      font-size: 18px;
      font-weight: 500;
      cursor: pointer;
      box-shadow: 0 4px 15px rgba(182, 191, 202, 0.267);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .start-btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 20px rgba(64, 150, 255, 0.4);
    }

    /* 响应式适配 - 移动端友好 */
    @media (max-width: 768px) {
      .top-nav {
        padding: 0 20px;
      }
      .nav-menu {
        gap: 20px;
      }
      .logo-text {
        font-size: 18px;
      }
      .nav-item {
        font-size: 14px;
      }
      .hero-title {
        font-size: 32px;
      }
      .hero-desc {
        font-size: 16px;
      }
      .start-btn {
        padding: 12px 30px;
        font-size: 16px;
      }
    }
  </style>
</head>
<body>
  <!-- 顶部导航栏 - 核心导航入口 -->
  <div class="top-nav">
    <!-- 平台Logo -->
    <div class="logo">
      <i class="fa-solid fa-file-invoice logo-icon"></i>
      <div class="logo-text">TradeWeb</div>
    </div>
    
    <!-- 核心导航菜单 -->
    <div class="nav-menu">
      <a href="index.html" class="nav-item active">首页</a>
      <a href="bill.html" class="nav-item">数字提单化</a>
      <a href="nft.html" class="nav-item">NFT铸造</a>
      <a href="finance.html" class="nav-item">融资管理</a>
      <a href="personal.html" class="nav-item">个人中心</a>
      <a href="team.html" class="nav-item">团队介绍</a>
    </div>
    
    <!-- 登录按钮 -->
    <button class="login-btn" onclick="window.location.href='login.html'">登录/注册</button>
  </div>

  <!-- 主体视觉区域 - 美观展示 -->
  <div class="hero-section">
    <div class="hero-content">
      <h1 class="hero-title">贸易提单<span>RWA数字化</span>解决方案</h1>
      <p class="hero-desc">
        一站式实现提单上传、结构化提取、NFT铸造与融资管理，
        打造高效、安全的贸易金融数字化平台
      </p>
      <button class="start-btn" onclick="window.location.href='bill.html'">立即体验</button>
    </div>
  </div>
</body>
</html>